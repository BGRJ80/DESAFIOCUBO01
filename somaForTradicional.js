const numeroInteiros = ["A", "B", "C", "D"];

function solucao(lista) {

    let soma = 0

    for (let posicao = 0; posicao < lista.length; posicao++) {
        console.log(`${posicao} - ${lista[posicao]}`)
        // console.log(i)

    }

    // console.log(soma)
}

solucao(numeroInteiros);

